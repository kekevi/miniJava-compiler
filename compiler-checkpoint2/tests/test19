class Statements {
  private int foo(int tamales, String party) {
    if (abc == 123) {
      doThis();
      doThat();
      multiline it = 145;

      while (89) {
        potato potato = potato[new Potato()]; /* weird, `new Constructor(args)` is not allowed */
        secondLine();
      }
    }
  }
}